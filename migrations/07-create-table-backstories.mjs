export async function up(sql) {
  await sql`
    CREATE TABLE backstories (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      class_id integer REFERENCES classes (id) ON DELETE CASCADE,
      origin_id integer REFERENCES origins (id) ON DELETE CASCADE,
      first_name_id integer REFERENCES first_names (id) ON DELETE CASCADE,
      last_name_id integer REFERENCES last_names (id) ON DELETE CASCADE,
      backstory text,
			verified boolean NOT NULL
      )
  `;
}

export async function down(sql) {
  await sql`
    DROP TABLE backstories
  `;
}
